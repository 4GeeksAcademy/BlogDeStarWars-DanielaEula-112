CUAL ES EL PRIMER PASO PARA UTILIZAR EL ESTADO GLOBAL: 
-import useGlobalReducer en el componente
-declaramos esa variable "const {store, dispatch} = useGlobalReducer()"
-creamos la funcion que me va a hacer el fetch 
" async function getCharacters(){
	try{
		let response = await fetch('https://www.swapi.tech/api/people',
		{method: 'GET'})
		let data = await response.json()
		dispatch({type: 'get_characters', payload: data.results});

		
  } catch (error){
	console.log(error);
  }
} "

-en el initialStore declaramos nuestros estados que debe seguir un formato tipo objeto :"export const initialStore=()=>{

  return{
    characters:[],
    addFav:[]
  }
}"

- creamos el CASE que se va a encargar de almacenar los datos 
"case 'get_characters':    //se escribe con snake_case //

      return {
        ...store,
       characters: action.payload
      };"

- verificar que me lleguen los casos con el console.log() => "  console.log("esto es mi store", store.characters); "
- una vez que este OK , utilizo .map() de "store.characters.map()" => "
return (
		<div className="text-center mt-5">
			<h1>CHARACTERS</h1>
			<div>
				{store.characters.length == 0 ? ( 
					<p>Cargando</p>
					 ) : (
					<div> 
						{store.characters.map((item) => (
							<CardCharacters name={item.name} id={item.uid} key={item.uid} />
						))}
					</div>
						 )}
			 
			</div>
		</div>
	);"

- utilizaré el GET con estado global para traerme los planetas, personajes, vehiculos...


//grid de las cards: 

<div className="card">
  <div className="card-body">
    <h1 className="card-title">CHARACTERS</h1>
    <div className="row">
      {store.characters.length == 0 ? (
        <p>Cargando</p>
      ) : (
        store.characters.map((item) => (
          <div className="col-md-4" key={item.uid}>
            <CardCharacters name={item.name} id={item.uid} />
          </div>
        ))
      )}
    </div>
  </div>
</div>


//Preguntas para continuar:
- ¿como hago para traerme las imagenes de cada uno de los personajes, vehiculos, planetas?
- ¿ como hago para que "learn more" me lleve a la vista "detalles"?
- ¿ como traer a detalles... 

url:
https://raw.githubusercontent.com/tbone849/star-wars-guide/refs/heads/master/build/assets/img/characters/1.jpg

planeta tattoine: 

<img 
  src={imagen} 
  onError={(e) => { e.target.src = rigoImageUrl; }} 
  className="card-img-top" 
  alt="..." 
/>

***El evento onError es un evento que se dispara cuando ocurre un error al cargar un recurso, como una imagen
https://static.wikia.nocookie.net/ptstarwars/images/8/82/Tatooine-TOR.jpg/revision/latest?cb=20160320020409

··· VISTA detalles ...
... Traer info en detalles para cada personaje, vehiculo, planeta...
... contador de favoritos a medida que se van agregando ...